@model IEnumerable<WebMercadoIGL2.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.endereco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.endereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cpfCliente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cpfCliente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cpfCliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
